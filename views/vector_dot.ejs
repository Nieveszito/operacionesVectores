<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Producto Punto | VectorSys</title>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <script>window.MathJax = { tex: { inlineMath: [['$', '$'], ['\\(', '\\)']] } };</script>
    <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
    
    <script src="https://cdn.plot.ly/plotly-2.24.1.min.js"></script>

    <style>
        :root {
            --bg-dark: #050505;
            --glass-panel: rgba(20, 20, 24, 0.7);
            --glass-border: rgba(255, 255, 255, 0.08);
            --accent-primary: #06b6d4; /* CYAN */
            --text-main: #ffffff;
            --text-muted: #94a3b8;
        }

        * { box-sizing: border-box; }
        body { 
            font-family: 'Inter', sans-serif; 
            background-color: var(--bg-dark); 
            color: var(--text-main); 
            margin: 0; padding: 0; 
            min-height: 100vh; 
            display: flex; flex-direction: column; align-items: center; justify-content: center; 
            overflow-x: hidden; 
        }

        /* Luces ambientales (Tono Cyan) */
        .ambient-glow { 
            position: fixed; top: -20%; left: -10%; width: 50vw; height: 50vw; 
            background: radial-gradient(circle, rgba(6, 182, 212, 0.15) 0%, transparent 70%); 
            filter: blur(80px); z-index: -1; pointer-events: none; 
        }
        
        /* Navbar */
        .navbar-island { 
            position: fixed; top: 20px; z-index: 100; 
            background: rgba(10, 10, 10, 0.8); backdrop-filter: blur(20px); 
            border: 1px solid rgba(255,255,255,0.1); border-radius: 100px; 
            padding: 8px 12px; display: flex; gap: 8px; align-items: center; 
            box-shadow: 0 10px 40px rgba(0,0,0,0.5); transition: transform 0.3s ease; 
        }
        .navbar-island:hover { transform: translateY(2px); }
        .nav-btn { 
            color: var(--text-muted); text-decoration: none; 
            width: 36px; height: 36px; border-radius: 50%; 
            display: flex; align-items: center; justify-content: center; 
            transition: 0.3s; font-size: 1rem; 
        }
        .nav-btn:hover { background: rgba(255,255,255,0.1); color: #fff; }
        .nav-btn.active { background: #fff; color: #000; }
        .nav-btn.close:hover { background: rgba(239, 68, 68, 0.2); color: #ef4444; }
        .divider { width: 1px; height: 16px; background: rgba(255,255,255,0.2); margin: 0 4px; }

        /* Layout */
        .main-container { 
            width: 90%; max-width: 1100px; 
            display: grid; grid-template-columns: 1fr 1.2fr; gap: 25px; 
            margin-top: 80px; animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1); 
        }
        @media (max-width: 850px) { .main-container { grid-template-columns: 1fr; margin-bottom: 50px; } }

        /* Paneles */
        .panel { 
            background: var(--glass-panel); backdrop-filter: blur(30px); 
            border: 1px solid var(--glass-border); border-radius: 24px; padding: 40px; 
            box-shadow: 0 20px 50px rgba(0,0,0,0.4); 
        }
        .panel-header { margin-bottom: 25px; }
        .panel-title { font-size: 1.5rem; font-weight: 600; margin: 0 0 5px 0; }
        .panel-subtitle { font-size: 0.85rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; }

        .input-group { margin-bottom: 25px; }
        .label-text { display: block; font-size: 0.85rem; color: var(--text-muted); margin-bottom: 10px; font-weight: 500; }
        
        .modern-input { 
            width: 100%; padding: 14px; 
            background: rgba(0, 0, 0, 0.3); border: 1px solid rgba(255,255,255,0.05); 
            border-radius: 12px; color: #fff; font-size: 1rem; font-family: 'Inter', sans-serif; 
            transition: 0.3s; 
        }
        .modern-input:focus { 
            outline: none; background: rgba(0,0,0,0.5); 
            border-color: var(--accent-primary); 
            box-shadow: 0 0 0 4px rgba(6, 182, 212, 0.15); 
        }

        .btn-primary { 
            width: 100%; padding: 18px; background: #fff; color: #000; 
            border: none; border-radius: 14px; font-size: 1rem; font-weight: 600; 
            cursor: pointer; transition: 0.3s; margin-top: 10px; 
        }
        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(255,255,255,0.15); }

        .panel-right { 
            position: relative; background: rgba(15, 15, 20, 0.4); 
            border: 1px dashed rgba(255,255,255,0.1); 
            align-items: center; text-align: center; justify-content: center; 
            display: flex; flex-direction: column; 
        }
        
        .panel-right.active { 
            background: linear-gradient(145deg, rgba(20,20,24,0.9), rgba(10,10,10,0.95)); 
            border: 1px solid var(--glass-border); border-top: 1px solid var(--accent-primary); 
            animation: slideIn 0.6s cubic-bezier(0.16, 1, 0.3, 1); 
            display: block; padding: 20px; 
        }
        
        /* Contenedor Gráfica */
        #plotDiv { width: 100%; height: 350px; border-radius: 12px; overflow: hidden; margin-top: 20px; }

        /* Animación Empty State (Pulse Cyan) */
        .empty-state-icon { font-size: 4rem; margin-bottom: 20px; animation: pulse-cyan 3s infinite ease-in-out; }
        @keyframes pulse-cyan { 
            0% { color: rgba(255,255,255,0.05); transform: scale(1); } 
            50% { color: #06b6d4; text-shadow: 0 0 30px rgba(6, 182, 212, 0.8); transform: scale(1.1); } 
            100% { color: rgba(255,255,255,0.05); transform: scale(1); } 
        }
        
        @keyframes fadeUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes slideIn { from { opacity: 0; transform: translateX(20px); } to { opacity: 1; transform: translateX(0); } }
    </style>
</head>
<body>
    <div class="ambient-glow"></div>
    
    <nav class="navbar-island">
        <a href="/dashboard" class="nav-btn"><i class="fa-solid fa-house"></i></a>
        <div class="divider"></div>
        <a href="/vector-sum" class="nav-btn"><i class="fa-solid fa-plus-minus"></i></a>
        <a href="/vector-dot" class="nav-btn active"><i class="fa-solid fa-circle-dot"></i></a>
        <a href="/vector-cross" class="nav-btn"><i class="fa-solid fa-xmark"></i></a>
        <div class="divider"></div>
        <a href="/dashboard" class="nav-btn close"><i class="fa-solid fa-xmark"></i></a>
    </nav>

    <div class="main-container">
        
        <div class="panel panel-left">
            <div class="panel-header">
                <h1 class="panel-title">Producto Punto</h1>
                <span class="panel-subtitle">Escalar y Ángulo $\vec{A} \cdot \vec{B}$</span>
            </div>
            
            <form action="/vector-dot" method="POST">
                <div class="input-group">
                    <label class="label-text">Vector $\vec{A}$ (separado por comas)</label>
                    <input type="text" class="modern-input" name="vecA" placeholder="Ej: 1, 0, 0" required value="<%= locals.vecA || '' %>">
                </div>
                <div class="input-group">
                    <label class="label-text">Vector $\vec{B}$ (separado por comas)</label>
                    <input type="text" class="modern-input" name="vecB" placeholder="Ej: 0, 1, 0" required value="<%= locals.vecB || '' %>">
                </div>
                
                <button type="submit" class="btn-primary">Calcular <i class="fa-solid fa-calculator" style="margin-left:8px"></i></button>
            </form>
        </div>

        <div class="panel panel-right <%= locals.resultado ? 'active' : '' %>">
            
            <% if (locals.resultado) { %>
                <div style="text-align: left;">
                    <span style="color:var(--accent-primary); font-size:0.8rem; font-weight:700; text-transform:uppercase;">Resultado Escalar</span>
                    <div style="font-size: 2.5rem; margin: 10px 0; font-weight: 600;">
                        $$ \vec{A} \cdot \vec{B} = <%= resultado %> $$
                    </div>

                    <div style="background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); border-radius: 12px; padding: 15px; display: flex; align-items: center; justify-content: space-between; margin-top: 15px;">
                        <span style="color: var(--text-muted); font-size: 0.9rem;">Ángulo entre vectores ($\theta$)</span>
                        <span style="font-size: 1.2rem; font-weight: 600; color: #fff;">
                            <%= angulo %>^\circ
                        </span>
                    </div>
                </div>
                
                <div id="plotDiv"></div>

                <script>
                    // Capturamos vectores (si existen, o 0)
                    // Como el producto punto se usa mucho para ángulos, visualizamos los vectores originales
                    let ax, ay, az, bx, by, bz;
                    
                    try {
                        const vecAStr = "<%= typeof vecA !== 'undefined' ? vecA : '0,0,0' %>";
                        const vecBStr = "<%= typeof vecB !== 'undefined' ? vecB : '0,0,0' %>";
                        
                        const arrA = vecAStr.split(',').map(Number);
                        const arrB = vecBStr.split(',').map(Number);

                        ax = arrA[0] || 0; ay = arrA[1] || 0; az = arrA[2] || 0;
                        bx = arrB[0] || 0; by = arrB[1] || 0; bz = arrB[2] || 0;
                    } catch(e) {
                        ax=0; ay=0; az=0; bx=0; by=0; bz=0;
                    }

                    // Definimos los trazos para Plotly
                    const traceA = { 
                        x: [0, ax], y: [0, ay], z: [0, az], 
                        mode: 'lines+markers', type: 'scatter3d', name: 'Vec A', 
                        line: {width: 8, color: '#06b6d4'} // Cyan
                    };
                    const traceB = { 
                        x: [0, bx], y: [0, by], z: [0, bz], 
                        mode: 'lines+markers', type: 'scatter3d', name: 'Vec B', 
                        line: {width: 8, color: '#a855f7'} // Morado para contraste
                    };
                    
                    const layout = {
                        margin: {l:0, r:0, b:0, t:0},
                        paper_bgcolor: 'rgba(0,0,0,0)', 
                        plot_bgcolor: 'rgba(0,0,0,0)',
                        scene: {
                            xaxis: {title: 'X', color:'white'}, 
                            yaxis: {title: 'Y', color:'white'}, 
                            zaxis: {title: 'Z', color:'white'},
                            bgcolor: 'rgba(0,0,0,0)'
                        },
                        legend: {x: 0, y: 1, font: {color: 'white'}}
                    };

                    Plotly.newPlot('plotDiv', [traceA, traceB], layout, {displayModeBar: false});
                </script>

                <a href="/vector-dot" style="display:block; text-align:center; margin-top:20px; color:#aaa; text-decoration:none;">
                    <i class="fa-solid fa-rotate-left"></i> Limpiar
                </a>

            <% } else { %>
                
                <div class="empty-state" style="display:flex; flex-direction:column; align-items:center; justify-content:center; height:100%;">
                    <i class="fa-solid fa-circle-dot empty-state-icon"></i>
                    <h3 style="margin:0;">Producto Punto</h3>
                    <p style="color:var(--text-muted); font-size:0.9rem;">Calcula proyecciones y ángulos.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>